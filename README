Welcome! now u can do :
        -u or --update          update the web, now!
        -v or --version         echo the version
        -d or --debug           open tomcat with debug mode
        -r or --replace         rollback to the last version
        -s or --show            show the web version
        -a or --all             display all versions

------------------------------------------------------------
For example:

autoproj -r 2.1                 u will rollback to the v2.1

-----------------------------------------------------------
If you see something like E00X,please call 189xxxxxxxx
changsha//

